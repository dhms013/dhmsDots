#!/bin/bash

# dhms:summary=Report whether a screen recording is active, as JSON for the status bar
# dhms:hidden=true

if pgrep -f "^gpu-screen-recorder" >/dev/null; then
  echo '{"text": "󰻂", "tooltip": "Stop recording", "class": "active"}'
else
  echo '{"text": ""}'
fi
