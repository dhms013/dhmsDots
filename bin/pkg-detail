#!/bin/bash

for pkg in "$@"; do
  if ! pacman -Q "$pkg" &>/dev/null; then
    gum log --level error "$pkg is not installed"
  else
    echo "$pkg is present..."
    paru -Qi $pkg | bat
  fi
done

exit 0
