#!/bin/bash

# dhms:summary=Remove packages with paru, silently skipping ones that are not installed
# dhms:args=<packages...>

for pkg in "$@"; do
  if pacman -Q "$pkg" &>/dev/null; then
    paru -Rns --noconfirm "$pkg"
    gum log --level info "Removed: $pkg"
  fi
done
