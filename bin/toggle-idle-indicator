#!/bin/bash

# dhms:summary=Report the idle-lock state as JSON for the status bar
# dhms:hidden=true

if pgrep -x hypridle >/dev/null; then
  echo '{"text": ""}'
else
  echo '{"text": "󱫖", "tooltip": "Idle lock disabled", "class": "active"}'
fi
