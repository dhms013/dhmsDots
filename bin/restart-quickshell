#!/bin/bash

# dhms:summary=Bounce the dhms shell (quickshell)
#
# The config lives at ~/.config/shell (stowed), not the quickshell default
# dir, so the relaunch must pass -p explicitly.

pkill -x quickshell
setsid uwsm-app -- quickshell -p "$HOME/.config/shell" >/dev/null 2>&1 &
