#!/bin/bash

if pgrep -x hypridle >/dev/null; then
  pkill -x hypridle
  # notify-send "󱫖    Stop locking computer when idle"
  restart-services quickshell
else
  uwsm-app -- hypridle >/dev/null 2>&1 &
  # notify-send "󱫖    Start locking computer when idle"
  restart-services quickshell
fi
