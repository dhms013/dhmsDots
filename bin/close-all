#!/bin/bash

# dhms:summary=Close every open Hyprland window

# Closes all client windows reported by hyprctl; unsaved work in them is lost.

hyprctl clients -j | jq -r '.[] | .address' | while read -r addr; do hyprctl dispatch closewindow address:"$addr"; done
