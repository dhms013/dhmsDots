#!/bin/bash

# dhms:summary=Suspend the system, enabling idle-locking first when hypridle is not running

if pgrep -x "hypridle" >/dev/null 2>&1; then
  systemctl suspend
else
  toggle-idle
  sleep 1
  systemctl suspend
fi
